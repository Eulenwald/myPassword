type Password = {
  "id": string,
  "name": string,
  "ip": string | null,
  "user_name": string | null,
  "wp": string,
  "description": string | null,
  "date": string,
  "date_of_change": string
};

export type {Password};
